const Images = require('../models/product.model');

let addImage=async (req,res,next)=>
{
    try
    {

    }
    catch(err)
    {
        next(err)
    }
}

let getImages=async (req,res,next)=>
    {
        try
        {
            
        }
        catch(err)
        {
            next(err)
        }
    }

    module.exports={
        addImage,
        getImages
    }